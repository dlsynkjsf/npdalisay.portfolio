# Public client key. Add this exact name to Vercel.
VITE_TURNSTILE_SITE_KEY=

# Server-only secrets. Never prefix these with VITE_ or commit real values.
TURNSTILE_SECRET_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
RATE_LIMIT_SALT=
RESEND_API_KEY=

# With Resend's test sender, the recipient must match the email on the Resend account.
RESEND_FROM_EMAIL="Portfolio <onboarding@resend.dev>"
CONTACT_TO_EMAIL=npdalisay@gmail.com

# Comma-separated. Add the final Vercel URL after the first deployment.
ALLOWED_ORIGINS=http://localhost:3000
